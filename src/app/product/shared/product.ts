export interface Product {
    id?: string,
    name?: string,
    description?: string,
    price?: number 
    categoryName?: string,
    providerId?: string,
    categoryId?: string,     
    image?: string    
  }